version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm install  # Changed from npm ci
    build:
      commands:
        - npm run build:production
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*