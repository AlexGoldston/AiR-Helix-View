version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci  # Clean install of node_modules
    build:
      commands:
        - npm run build:production
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*